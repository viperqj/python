# 什么叫模块
    # ·模块和包
    # ·模块英文为Modules·函数与模块的关系
        # ·一个模块中可以包含N多个函数
    # ·在Python中一个扩展名为.py的文件就是一个模块
    # ·使用模块的好处
        # ·方便其它程序和脚本的导入并使用
        # ·避免函数名和变量名冲突
        # ·提高代码的可维护性
        # ·提高代码的可重用性
# 导入模块
#     import 模块名称 as 别名
#     from 模块名称 import 函数、变量、类
import math
print(math)
print(type(math))
print('-----------------------------------------------------------------')
# print(dir(math))
print(math.ceil(9.00001))
print(math.floor(0.9999))









